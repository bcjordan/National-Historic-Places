<!DOCTYPE html>
<html>
<head>
<meta name="viewport" content="initial-scale=1.0, user-scalable=no" />
<style type="text/css">
  html { height: 100% }
  body { height: 100%; margin: 0px; padding: 0px }
  #map_canvas { width:50%; height:50%;float: left }
  #last_locations { width:50%; height:50%; float: right }
</style>
<script type="text/javascript"
    src="http://maps.google.com/maps/api/js?sensor=false">
</script>
<script type="text/javascript">
  function initialize() {
    var latlng = new google.maps.LatLng(<%= "#{@user_location.lat}, #{@user_location.lng}" %>, 150.644);
    var myOptions = {
      zoom: 8,
      center: latlng,
      mapTypeId: google.maps.MapTypeId.ROADMAP
    };
    var map = new google.maps.Map(document.getElementById("map_canvas"),
        myOptions);
  }

</script>
</head>
<body onload="initialize()">
  <p id="notice"><%= notice %></p>
  
  <div id="map_canvas"></div>
  <div id="last_locations">
  Your name:              <%= @user.name %> <br/>
  Your current latitude:  <%= @user_location.lat %> <br/>
  Your current longitude: <%= @user_location.lng %> <br/>
  Your current IP:        <%= @ip %> <br/>
  </div>

<br/>
<%= "#{@user.name}'s nearest #{pluralize(@nearest.size, "landmark")}:" %> <br/>

<div id="listing">
<% @nearest.each do |lmark| %>
<%= "Landmark #{lmark.name} is at #{lmark.lat}, #{lmark.lng}" %> <br/>
<% end %>
</div>

<%= "#{@user.name}'s last #{pluralize(@last_10.size, "location")}:" %>
<%= link_to "JSON", "/my_recent.json" %>, 
<%= link_to "XML", "/my_recent.json" %>

</body>
</html>